# Backend Guide

## Overview
This document covers the backend layout and how API code is organized.

## Structure
- `src/server/api`: API entry points and route handlers
- `src/lib`: Shared services and utilities
- `src/middlewares`: Request/response middleware
- `src/types`: Shared types and interfaces

## Local Development
Add backend setup steps here (database, env vars, seeds).
