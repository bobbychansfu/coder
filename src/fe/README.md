# Frontend Features

Feature-specific UI lives under `src/fe/<feature>`.
Shared, reusable UI lives under `src/fe/shared`.

Example:
- `src/fe/dashboard` for the dashboard page
- `src/fe/contests` for contests
