# Frontend Features

Feature-specific UI lives under `src/fe/<feature>/`.
Shared, reusable UI lives under `src/fe/shared`.

Example:
- `src/fe/dashboard/page/index.ts` for the dashboard entry point
- `src/fe/contests/page/index.ts` for contests
